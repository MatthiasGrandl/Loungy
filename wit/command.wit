package loungy:command;

interface command {
    use shared.{img,metadata,callback-type,callback};
    
    init: func() -> metadata;
    run: func();
    call: func(callback: callback) -> callback-type;
}

world loungy {
    import host;
    export command;
}
